# KAIROS Environment Variables Template
# Copy this to .env and fill in the values

# API Security
# Generate a strong key for production (e.g., openssl rand -hex 32)
API_KEY=kairos_dev_key_2026

# Rate Limiting
# Format: "count/time_period" (e.g., "10/minute", "1000/hour")
RATE_LIMIT=20/minute

# ML Engine Config
TAU_LOW=0.20
TAU_HIGH=0.80

# MLflow Tracking
MLFLOW_TRACKING_URI=http://localhost:5050
